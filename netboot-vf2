env set bootargs "nvme_poll root=block3:0 serial_debug"; pci enum; env set serverip 192.168.178.30; if dhcp ${kernel_addr_r} ${serverip}:Kernel.efi; then bootefi ${kernel_addr_r} ${fdtcontroladdr}; fi
